package com.embarrasdf.palette.theme.semantic.animation.infinite

/**
 * Animation schemes built from infinite (non-terminating) specs — loops / ambient motion. There are
 * no infinite tokens yet; this is the placeholder the grouping hangs off of. When the first infinite
 * scheme is added, promote this to a `data class` holding it.
 */
data object InfiniteAnimationScheme
